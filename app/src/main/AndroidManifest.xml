<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />
    <uses-permission-sdk-23 android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logoapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logoapp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rate"></activity>
        <activity android:name=".tempat" />
        <activity android:name=".lupa" />
        <activity android:name=".loading" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LokGudang"
            android:label="@string/title_activity_lok_gudang" />
        <activity
            android:name=".LokPuteran"
            android:label="@string/title_activity_lok_puteran" />
        <activity
            android:name=".LokKristalisasi"
            android:label="@string/title_activity_lok_kristalisasi" />
        <activity
            android:name=".LokPenguapan"
            android:label="@string/title_activity_lok_penguapan" />
        <activity
            android:name=".LokPemurnian"
            android:label="@string/title_activity_lok_pemurnian" />
        <activity
            android:name=".LokPemerahan"
            android:label="@string/title_activity_lok_pemerahan" />
        <activity android:name=".step6" />
        <activity android:name=".step5" />
        <activity android:name=".step4" />
        <activity android:name=".step3" />
        <activity android:name=".step2" />
        <activity android:name=".Step1" />
        <activity android:name=".pelajari" />
        <activity android:name=".Sejarah" />
        <activity android:name=".alamat" />
        <activity
            android:name=".lokasi"
            android:label="@string/title_activity_lokasi" />
        <activity android:name=".TentangMadubaru" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
    </application>

</manifest>